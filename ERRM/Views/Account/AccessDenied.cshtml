@{
    ViewData["Title"] = "Access denied";
}

<section class="py-4">
    <div class="info-panel info-panel--compact">
        <span class="eyebrow">Authorization</span>
        <h1 class="display-6">Access denied</h1>
        <p class="text-muted mb-4">
            Your account is authenticated, but it does not have access to this page.
        </p>
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Back to home</a>
    </div>
</section>
